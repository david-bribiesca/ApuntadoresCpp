#include "MatrixOp.h"
#include <iostream>
using namespace std;
int main(){
    /*IMatrix* mat = new MatrixOp(2, 2);
    MatrixOp* realMat = dynamic_cast<MatrixOp*>(mat);
    realMat->set(0, 0, 1.0);
    realMat->set(0, 1, 2.0);
    realMat->set(1, 0, 3.0);
    realMat->set(1, 1, 4.0);*/

    // No supe resolver
    return 0;
}